# Rust / Tauri build output
/src-tauri/target/
/src-tauri/gen/schemas/

# Node
/node_modules/

# Vite build output (frontendDist); legacy/index.html is the pre-Vite copy
# kept for reference, not this.
/dist/

# Local music and analysis cache used for testing
/testdata/

# Session handoff notes and deferred-issue list (local, not for publication)
/HANDOFF.md
/ISSUES.md

# win-run.sh success fingerprint (WSL-local; skip rebuild when unchanged)
/.win-run.stamp

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/

# Desktop run data (GUI=1 ./dev.sh): music dropped in by hand + analysis cache
/.desktop-data/

# MSIX pack intermediates / output
/packaging/msix/out/
/packaging/msix/staging/

# Release signing keys (host-only, never commit)
/.secrets/
