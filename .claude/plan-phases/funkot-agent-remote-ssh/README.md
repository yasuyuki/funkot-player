# funkot-agent-remote-ssh

A same-distro SSH endpoint lets Cursor select funkot-agent explicitly without
changing the WSL default user or opening a local/UNC workspace.

| # | File | Content | Status |
|---|---|---|---|
| 01 | phase-01-agent-ssh-endpoint.md | loopback-only SSH endpoint and agent login verification | Complete |

Execute a phase only when the user names its phase file.
