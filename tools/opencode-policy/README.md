# Local OpenCode CLI

This directory pins OpenCode only. Credentials, configuration, sessions, and
logs belong under `/home/funkot-agent/`, never in this repository. Run it only
through `tools/agent-isolation/launch-agent.sh`.
